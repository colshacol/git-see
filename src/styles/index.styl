@require './global.styl';
@require './type.styl';

html, body, div, header, footer, nav, aside, section {
  box-sizing: border-box;
  margin: 0;
  padding: 0;
}

:root {
  font-family: $primary-font;
}

h1, h2, h3, h4, h5, h6 {
  font-family: $secondary-font;
}
